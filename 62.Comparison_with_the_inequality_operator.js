// TODO : Add the inequality operator != in the if statement so that the function will return the string Not Equal when val is not equivalent to 99.

function testNotEqual(val) {
  if (val != 99) {
    console.log('Not Equal');
  } else {
    console.log('Equal');
  }
}

testNotEqual(10);
